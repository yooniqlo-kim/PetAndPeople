spring:
  datasource:
    url: jdbc:mysql://localhost:3306/petandpeople
    username: petandpeople
    password: 1234
  driver-class-name: com.mysql.cj.jdbc.Driver
  hikari:
    maximum-pool-size: 10

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    database-platform: org.hibernate.dialect.MySQL8Dialect

  server:
    servlet:
      context-path: /api